[tox]
envlist = py39
skipsdist = True

[testenv]
deps =
    pytest
    pytest-cov
    sqlalchemy  # Adicionando SQLAlchemy explicitamente
    pymysql # Adicionando pymysql explicitamente
commands = pytest --cov=src --cov-report=xml --cov-config=tox.ini --cov-branch
